# 🧪 دليل اختبار أمان عزل البيانات (RLS Testing Guide)

**الهدف:** التأكد من أن كل مستخدم يرى بيانات شركته (Organization) فقط، وأن الصلاحيات (Roles) تعمل بشكل صحيح.

**الأداة المستخدمة:** Supabase SQL Editor -> JWT Impersonation

---

### الخطوة 1: الحصول على معرفات المستخدمين للاختبار

أولاً، نحتاج إلى `auth_id` لمستخدمين مختلفين من شركات مختلفة. قم بتشغيل هذا الكود في SQL Editor للحصول عليهم. (استبدل الإيميلات بإيميلات حقيقية من قاعدة بياناتك).

```sql
-- ابحث عن مستخدمين من شركات مختلفة لاختبارهم
-- استبدل الإيميلات التالية بإيميلات حقيقية موجودة لديك
SELECT 
  id, 
  auth_id, 
  email, 
  role, 
  org_id 
FROM employees 
WHERE email IN ('admin_company_a@example.com', 'employee_company_a@example.com', 'admin_company_b@example.com');
```

انسخ قيم `auth_id` التي ستظهر لك.

---

### الخطوة 2: تنفيذ سيناريوهات الاختبار

لكل سيناريو، اتبع الخطوات التالية:
1.  اذهب إلى **SQL Editor**.
2.  في الأعلى، اضغط على **"Authenticate"** أو **"JWT Impersonation"**.
3.  الصق `auth_id` الخاص بالمستخدم الذي تريد اختبار صلاحياته.
4.  شغّل أوامر `SELECT` الموضحة أدناه ولاحظ النتائج.

---

#### ✅ السيناريو 1: مدير من الشركة "أ"
- **الشخصية:** مدير (Admin) من الشركة "أ".
- **الاختبار:** `SELECT * FROM employees;`
- **النتيجة المتوقعة:** يجب أن ترى **فقط** الموظفين التابعين للشركة "أ". لا يجب أن يظهر أي موظف من أي شركة أخرى.

---

#### ✅ السيناريو 2: موظف عادي من الشركة "أ"
- **الشخصية:** موظف عادي (Employee) من الشركة "أ".
- **الاختبار الأول:** `SELECT * FROM employees;`
- **النتيجة المتوقعة:** يجب أن ترى **فقط** الموظفين التابعين للشركة "أ".
- **الاختبار الثاني (الأهم):** `SELECT * FROM payroll_records;`
- **النتيجة المتوقعة:** يجب أن تحصل على **صفر من النتائج (0 rows)**. هذا يثبت أن سياسة حماية البيانات المالية تعمل وتمنع الموظف العادي من رؤية الرواتب.

---

#### ✅ السيناريو 3: مدير من الشركة "ب"
- **الشخصية:** مدير (Admin) من الشركة "ب".
- **الاختبار:** `SELECT * FROM employees;`
- **النتيجة المتوقعة:** يجب أن ترى **فقط** الموظفين التابعين للشركة "ب". هذا يثبت أن بيانات الشركة "أ" معزولة تماماً عن الشركة "ب".

---

**إذا نجحت جميع هذه السيناريوهات، فهذا يعني أن نظام الأمان لديك يعمل بكفاءة 100%.**